/**
 * Versão do prompt/examples de moderação atualmente em uso.
 *
 * Troca manual (ex.: via `/tune-moderation`): edita este arquivo apontando
 * pro novo `.md` em `versions/`. Escolha explícita > convenção: greppable,
 * reviewable e falha em tempo de boot se o arquivo não existir.
 */
export const ACTIVE_VERSION = "2026-04-v5";
